{
  "name": "medical-triage-system-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Medical triage system monorepo (web + api + shared packages)",
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "prettier -w ."
  },
  "dependencies": {
    "@nestjs/event-emitter": "^3.0.1"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "husky": "^9.1.7"
  }
}
